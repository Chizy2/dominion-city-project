{
  "name": "dominion-city-directory",
  "version": "1.0.0",
  "description": "Modern business and skilled professionals directory web app",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "build:all": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "start:prod": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "keywords": ["directory", "business", "professionals"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
